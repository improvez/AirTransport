@model AirTransport.Models.ListPassengersFlight

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ListPassengersFlight</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdFlight" />
            <div class="form-group">
                <label asp-for="IdPassenger" class="control-label"></label>
                <select asp-for="IdPassenger" class="form-control" asp-items="ViewBag.IdPassenger"></select>
                <span asp-validation-for="IdPassenger" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="IsWindowSeat" /> @Html.DisplayNameFor(model => model.IsWindowSeat)
                </label>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="IsRight" /> @Html.DisplayNameFor(model => model.IsRight)
                </label>
            </div>
            <div class="form-group">
                <label asp-for="SeatNumber" class="control-label"></label>
                <input asp-for="SeatNumber" class="form-control" />
                <span asp-validation-for="SeatNumber" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
